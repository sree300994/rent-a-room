<h2>Listing Rooms</h2>

<% @rooms.each do |room| %>
	<li>
		<p> Name : <%= room.name %> </p>
		<p> Description : <%= room.description %> </p>
		<p> Address : <%= room.address %> </p>
		<%= link_to "Show", room_path(room.id) %>
	</li>
<% end %>
